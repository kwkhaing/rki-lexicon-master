# Core dependencies
pandas>=1.3.0
numpy>=1.21.0
pytest>=6.0.0
pyyaml>=5.4.0
unidecode>=1.3.0

# Data processing
python-dateutil>=2.8.0
pytz>=2023.3

# Testing
pytest-cov>=4.0.0

# IPA processing (compatible with Python 3.11)
# ipapy>=0.0.9  # Remove or comment out - has version constraints

# Note: sqlite3 is built into Python, no need to install
